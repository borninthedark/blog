# === Build & Cache directories ===
/dist/                    # Distribution directory
/tmp/                     # Temporary files
.cache/                   # Cache files (various tools)

# === Dependency directories ===
/node_modules/            # JavaScript dependencies
/.yarn/                   # Yarn cache
/vendor/                  # Go dependencies (if using Hugo modules)

# === Logs & Debugging ===
*.log                     # Log files
*.lock                    # Lock files (e.g., package-lock.json, yarn.lock)
*.pid                     # Process ID files
*.seed                    # DB seed files

# === Environment Variables & Secrets ===
.env                      # Local environment config
.env.*                    # Variants like .env.local, .env.production
*.pem                      # SSL Keys & certs
*.key                      # Private keys

# === IDE & Editor Files ===
.vscode/                  # VS Code settings
.idea/                    # JetBrains (WebStorm, PyCharm, etc.)
*.sublime-project         # Sublime Text project files
*.sublime-workspace       # Sublime Text workspace files
*.code-workspace          # VS Code workspace settings

# === OS & System Files ===
.DS_Store                 # macOS metadata
Thumbs.db                 # Windows metadata
*.bak                     # Backup files
*.swp                     # Vim swap files
*.swo                     # Vim swap files
.nfs*                     # Linux NFS temp files
._*                       # macOS metadata
.Trashes                  # macOS trash files

# === Git & Version Control ===
.gitignore                # This file should be committed, but no additional ones
.gitattributes            # Git attributes (usually commit this though)
*.orig                    # Merge conflict resolution files

# === Miscellaneous ===
/coverage/                # Test coverage reports
*.test.js                 # Temporary test files
*.spec.js                 # Test specification files (keep only if necessary)
